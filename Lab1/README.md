В этой лабе у меня 9 вариант и необходимо было заполнять матрицу 10 на 10, выбирая направление с помощью D-pad. Сразу скажу, что разбирался с теорией и в принципе с кодом
с помощью нейронки, так как без нее не особо понятно, откуда брать теорию. В начале программы задаем с помощью задания констант цвета и адреса матрицы и дпада. Тут у меня
возникали первые проблемы, так как адреса не записывались, если использовать la, но проблема была решена)
Потом идет по сути основная функция, которая записывает значение переменной t5 в зависимости от нажатой кнопки дпада. В зависимости от направления запускается одна из функций:
с права налево, или с лева направо. По сути отличаются эти функции только начальным положением светодиода (начало или конец матрицы). Вычисляется местоположение начального
диода, а потом в него заносится нужный цвет и переходит на следующий диод в строке. После того как заполенна строка снова вступает в игру цикл ожидания нажатия дпада. 
В целом все, возможно есть пути легче, но так как это первая лаба, то тут я не искал оптимальных вариантов, а пытался разобраться в самой сути всего происходящего.